# Enterprise Agent Platform frontend

Next.js, TypeScript, and Tailwind dashboard for the Phase 7 backend.

```bash
npm install
npm run dev
```

The browser uses the same-origin `/api/backend/*` proxy. Set `BACKEND_URL`
to the backend origin when it is not available at `http://localhost:8000`.
